import { HoraMilitarPipe } from './hora-militar.pipe';

describe('HoraMilitarPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraMilitarPipe();
    expect(pipe).toBeTruthy();
  });
});
